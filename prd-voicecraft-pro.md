# VoiceCraft Pro — 产品需求文档（PRD）

> **版本**：v1.0  
> **日期**：2026-04-25  
> **状态**：Draft  
> **目标用户**：播客制作人、有声书团队、短视频配音创作者、广告/广播剧制作方  

---

## 一、产品定位与价值主张

| 维度 | 说明 |
|------|------|
| **一句话定位** | 面向音频内容生产者的"可视化配音工作站"——把长文稿变成多角色有声剧，所见即所得。 |
| **核心痛点** | 传统 TTS 工具：① 无法区分多角色台词；② 改一句就要重跑全文；③ 音频与文稿脱节，对字幕困难；④ 缺少画布级可视化编排。 |
| **价值主张** | 文稿 → 角色识别 → 分角色配音 → 单句精修 → 自动字幕 → 画布导出，全流程在一个页面完成，支持第三方顶级音色。 |

---

## 二、核心功能模块

### M1. 文稿角色引擎（Script & Character Engine）

| 功能点 | 详细描述 | 优先级 |
|--------|---------|--------|
| **文稿导入** | 支持粘贴文本、上传 .txt / .docx / .pdf / .srt / .csv 台词本。 | P0 |
| **自动角色识别** | NLP 解析文稿中的角色名（如"小明："、"A[旁白]"、**加粗角色** 等），自动提取角色列表。支持自定义正则/分隔符规则。 | P0 |
| **角色管理面板** | 左侧边栏展示识别出的角色清单，支持：重命名、合并同义词（如"小明"和"明哥"）、新增虚拟角色、删除误识别项。 | P0 |
| **文稿编辑器** | 中间主区域为行级/句级编辑器。每行显示：行号 + 角色标签 + 台词文本。支持直接修改文本、拖拽换序、拆分/合并句子。 | P0 |

### M2. 音色与 TTS 引擎（Voice Engine）

| 功能点 | 详细描述 | 优先级 |
|--------|---------|--------|
| **第三方音色接入** | 内置 ElevenLabs 作为首选音色提供商（需用户填入 API Key）。支持：Voice ID 选择、模型选择（Multilingual v2 / Turbo）、稳定性/相似度/风格化滑块。 | P0 |
| **角色-音色绑定** | 每个角色可独立绑定：① 一个主音色；② 一个备选音色（用于情绪切换）。支持试听按钮（仅生成当前句 3s 预览）。 | P0 |
| **全局音色库** | 支持接入更多 TTS 服务商：Azure TTS、Amazon Polly、Google Cloud TTS、MiniMax、阿里通义等。用户可在设置里配置多个 Provider 的 Key，音色统一在角色面板下拉选择。 | P1 |
| **单句重新生成** | 点击任意句子的"🔁"按钮，仅对该句调用 TTS API，音频流式替换，不触发全文重算。保留历史版本（最多 5 版，可回滚）。 | P0 |
| **批量生成策略** | 支持"整稿生成"与"逐句流式生成"两种模式。生成过程中显示队列进度、预估剩余时间、API 费用实时估算（基于 ElevenLabs Characters 计费）。 | P0 |
| **SSML / 标签支持** | 高级用户在台词中可插入 `<break time="500ms"/>`、`<prosody rate="slow">` 等 SSML 标签（按 Provider 支持程度透传）。 | P2 |
| **情绪/口音微调** | 若 Provider 支持（如 ElevenLabs Voice Design / Accent 参数），在角色设置里提供口音（美式/英式/澳式/中式等）与情绪预设（平静/激动/严肃/温柔）下拉框。 | P1 |

### M3. 音频画布与时间轴（Audio Canvas & Timeline）

| 功能点 | 详细描述 | 优先级 |
|--------|---------|--------|
| **空白画布创建** | 导入/生成音频后，底部自动展开"时间轴画布"，底色默认深灰（#1a1a1a），支持在设置里更换为：白/浅灰/蓝/自定义 HEX。 | P0 |
| **轨道系统** | 画布分多轨道：① 主配音轨（每角色独立子轨道，可用颜色区分）；② BGM 轨（导入背景音乐，支持淡入淡出）；③ SFX 轨（音效轨）。 | P1 |
| **波形可视化** | 音频以波形图展示在时间轴上，支持缩放（Ctrl+滚轮）、拖拽裁剪、静音某片段、复制粘贴音频块。 | P1 |
| **自动对齐** | 根据文稿行序，自动将生成的音频片段按顺序排列在时间轴上，片段间隙默认 0.3s（可全局调整）。 | P0 |
| **实时预览** | 点击画布播放头或按空格键，从当前位置播放合成音频；支持循环播放选区。 | P0 |

### M4. 自动字幕与高亮（Subtitle & Highlight）

| 功能点 | 详细描述 | 优先级 |
|--------|---------|--------|
| **自动字幕生成** | 基于 ASR（Whisper 本地 / API）对最终合成音频进行时间戳对齐，生成 .srt / .vtt 格式。对短句可直接用文稿原文与音频时长做粗略映射（快），长句再用 Whisper 精对（准）。 | P0 |
| **文稿高亮同步** | 播放音频时，文稿编辑器中当前朗读的句子实时高亮（背景色 pulse），且自动滚动到视口中央。 | P0 |
| **字幕样式设置** | 支持设置字幕字体、字号、颜色、描边、位置（上/中/下）、背景框透明度。实时预览样式效果。 | P1 |
| **字幕编辑器** | 在时间轴上以"字幕块"形式展示，可拖拽调整起止时间、双击修改文本、拆分长句。修改后同步回文稿。 | P1 |

### M5. 单句精修与版本控制（Sentence-level Polish）

| 功能点 | 详细描述 | 优先级 |
|--------|---------|--------|
| **句子级修改** | 双击文稿中任意句子即可编辑文本；修改后该句旁边出现"🔄 重新生成"提示，点击后单句 TTS。 | P0 |
| **断句/合句** | 在句子间按 Enter 拆分、按 Delete 合并（若两句同属一个角色）。拆分后自动分配时序。 | P0 |
| **历史版本** | 每句的音频保留最近 5 个生成版本，支持"试听旧版"和"回滚到旧版"。文稿编辑也支持 Cmd+Z 撤销。 | P1 |
| **标记/批注** | 对句子打标签："待重录"、"音量低"、"语速快"等，方便批量筛选处理。 | P1 |

### M6. 导入与导出（I/O）

| 功能点 | 详细描述 | 优先级 |
|--------|---------|--------|
| **音频导入** | 支持导入外部 .mp3 / .wav / .m4a，自动放置到画布 BGM 轨或主轨，并可选"从音频反推字幕"（Whisper ASR）。 | P0 |
| **工程文件** | 导出/导入 `.voicecraft` 工程文件（JSON + 音频资源包），包含全部角色设置、音色配置、时间轴、字幕样式。 | P1 |
| **最终导出** | 一键导出：① 合成音频（.mp3 / .wav）；② 字幕文件（.srt / .vtt / .ass）；③ 分轨音频包（每个角色的独立 .wav，供后期混音）；④ 视频草稿（带字幕烧录的 .mp4，用于预览）。 | P0 |

---

## 三、产品架构（Web 优先，纯客户端渲染 + 后端代理）

```
┌─────────────────────────────────────────────────────────┐
│                    VoiceCraft Pro (Web App)               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 文稿编辑器 │  │ 角色面板  │  │ 音色配置  │  │ 导出面板  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│  ┌─────────────────────────────────────────────────────┐│
│  │              音频画布 / 时间轴 / 波形                  ││
│  │  [配音轨: 角色A] [配音轨: 角色B] [BGM轨] [SFX轨]     ││
│  └─────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────┐│
│  │              字幕轨道（中/英双语，与音频对齐）          ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
  ┌──────────┐     ┌──────────┐     ┌──────────┐
  │ ElevenLabs│     │ Whisper  │     │  后端代理   │
  │   API     │     │ (API)    │     │ (转存/转发)│
  │ (平台Key)  │     │          │     │           │
  └──────────┘     └──────────┘     └──────────┘
```

**技术选型**：
- **前端**：React + TypeScript + Web Audio API + Wavesurfer.js（波形可视化）
- **后端**：极简 Node.js / Python FastAPI，仅做两件事：① ElevenLabs API 代理（隐藏平台 Key，加请求限流）；② 音频文件临时存储（生成时缓存，7 天自动清理）。
- **ASR**：后端调用 Whisper API（OpenAI 或自部署），比浏览器 WASM 更稳定、支持长音频。浏览器端只做结果接收与展示。
- **画布**：纯 CSS + Canvas 2D 实现可视化排列，不做专业 DAW 级混音引擎。

---

## 四、用户旅程（User Journey）

```
1. 打开工具 → 2. 粘贴/导入文稿 
   → 3. 系统自动识别角色（或手动标注）
   → 4. 为每个角色选择音色（试听+绑定）
   → 5. 点击"生成音频"（全文或逐句）
   → 6. 画布自动铺好音频块 + 波形
   → 7. 播放检查，发现某句不佳
   → 8. 双击修改文本 或 调整音色参数 → 单句重新生成
   → 9. 自动字幕对齐 → 微调字幕时间戳
   → 10. 导出成品音频 + 字幕 + 分轨包
```

---

## 五、页面结构与导航

| 页面/区域 | 说明 |
|-----------|------|
| **工作台（Workshop）** | 主界面，左侧角色面板 + 中间文稿 + 底部画布时间轴。 |
| **音色商店（Voice Store）** | 浏览/搜索所有已接入 Provider 的音色，支持收藏、试听、按语言/性别/风格筛选。 |
| **设置（Settings）** | API Key 管理、默认画布底色、默认字幕样式、计费看板、快捷键配置。 |
| **项目管理（Projects）** | 工程文件列表，支持搜索、最近打开、云端同步（若开启）。 |

---

## 六、补充逻辑（产品完善项）

| 编号 | 补充逻辑 | 说明 |
|------|---------|------|
| 7.1 | **智能停顿** | 根据标点符号（句号/问号/感叹号）自动在角色句间插入 0.2~0.5s 静音；段落之间插入 0.8s。用户可在角色设置里自定义"呼吸感"。 |
| 7.2 | **对话模式 vs 旁白模式** | 角色面板支持设置"对话角色"（有交互感，句尾轻微降调）与"旁白角色"（平稳叙述），影响生成时的 prompt/prosody。 |
| 7.3 | **音量/语速全局曲线** | 时间轴支持画"自动化曲线"（类似 DAW），可整体压低某角色的音量包络，或局部加速 1.2x。 |
| 7.4 | **BGM 智能闪避** | 检测到角色说话时，BGM 自动降低 -12dB；说话结束后 0.5s 内恢复。支持阈值与恢复速度自定义。 |
| 7.5 | **协作与审阅** | 工程文件生成分享链接，审阅者可在网页端标注"这句语速太快"，制作人在原工程收到批注并一键定位。 |
| 7.6 | **快捷键体系** | 空格（播放/暂停）、T（对选区生成 TTS）、R（单句重新生成）、Ctrl+Enter（全文生成）、S（拆分句子）。 |
| 7.7 | **计费透明化** | 实时展示 ElevenLabs 已消耗 Characters / 剩余额度 / 预估费用；接近上限时弹窗提醒。 |
| 7.8 | **模板市场** | 新手模板：有声书模板（旁白+对话）、播客模板（双人对话+开场 BGM）、广告模板（紧张 BGM+旁白）。 |
| 7.9 | **批量音色替换** | 将"角色 A 的 ElevenLabs Voice A"一键替换为"Voice B"，已生成音频批量重新跑（带确认弹窗）。 |
| 7.10 | **质量检测** | 生成完成后自动扫描：① 是否有句子为空白音频；② 是否有句子音频过长（可能 SSML 失效）；③ 标红提示用户。 |
| 7.11 | **多语言混合支持** | 若文稿包含中英混杂，自动检测并调用支持多语言的模型（ElevenLabs Multilingual v2），或在句子级指定语言。 |
| 7.12 | **画布导出为视频** | 除音频外，支持将中英双语字幕 + 纯色/图片背景渲染为 1080p MP4，直接用于短视频平台上传。 |
| 7.13 | **双语字幕模式** | 字幕轨道同时显示中/英两行，支持分别设置字体大小、颜色、位置偏移。英文可自动翻译或手动输入对照稿。 |

---

## 七、MVP 范围与迭代路线

### Phase 1（MVP，4~6 周）
- 文稿导入 + 自动角色识别
- 角色-音色绑定（ElevenLabs 单 Provider，平台统一采购 Key）
- 单句/全文生成 + 单句重新生成
- 基础画布（底色可调）+ 自动音频排列
- 播放同步文稿高亮
- 导出：合成音频 + 基础 .srt（中英文双语字幕）

### Phase 2（1~2 个月后）
- 自动字幕（Whisper ASR 精对时间戳）
- 多 Provider 音色接入
- 字幕样式设置 + 视频草稿导出
- 工程文件保存/打开

### Phase 3（3~4 个月后）
- 多轨道（BGM / SFX）
- 协作审阅
- 波形级剪辑
- 模板市场

---

## 八、竞品对标

| 产品 | 优势 | 我们的差异化 |
|------|------|-------------|
| ElevenLabs Projects | 原生音色好，支持章节管理 | 我们更强调"多角色分轨+画布可视化+字幕同步" |
| 剪映（文本朗读） | 国内方便，一键字幕 | 我们不绑定视频剪辑，专注"长文稿+多角色+精细调音" |
| Descript Overdub | 音频编辑强 | 我们的角色-音色绑定和重新生成更轻量、更面向有声书/广播剧 |
| 微软 Azure Speech Studio | 企业级，多语言 | 我们更面向创作者，UI 更轻，第三方音色可扩展 |

---

## 九、已确认的产品决策

| 问题 | 决策 |
|------|------|
| **1. 产品形态** | **优先 Web 应用**（浏览器运行），部署简单、跨平台。后期如需离线可再封装 Electron。 |
| **2. ElevenLabs Key** | **平台统一采购**，后端做代理转发，前端不暴露 Key。自建简单限流/配额机制，防止用量突增。 |
| **3. 画布深度** | **只做到可视化排列级别**，不做 DAW 级多轨混音。画布核心作用：看波形、调顺序、对齐字幕。BGM/SFX 支持简单叠加即可。 |
| **4. Whisper ASR** | **后端 GPU API 跑**（浏览器 WASM 对长音频性能差、内存受限）。后端用 OpenAI Whisper API 或自部署 Whisper，前端只管收结果。 |
| **5. 商业化** | **自用为主，暂不考虑商业化**。省去支付网关、计费系统、用户权限的复杂度，专注把工具做好用。 |

### 基于决策的架构要点

- **极简后端**：只做代理 + 音频缓存，不需要用户系统、不需要计费、不需要云同步。
- **ElevenLabs 配额管理**：后端用 Redis / 内存计数器做简单的 Characters 用量统计，超阈值时返回友好提示。
- **双语字幕流程**：文稿原文为中文时，可选接入翻译 API（DeepL / 通义）生成英字，再与 ASR 时间戳对齐。
- **本地优先存储**：工程文件和音频全部存在浏览器 IndexedDB（或用户指定的本地目录），无后端持久化压力。

---

*PRD 完毕。如需细化某个模块的交互流程图或接口定义，可以继续往下拆。*
